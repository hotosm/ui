name: 🚩 Publish to npmjs.org

on:
  release:
    types: [published]
  # Allow manual trigger (workflow_dispatch)
  workflow_dispatch:

jobs:
  steps:
    publish_to_npmjs:
      uses: ./npm_publish.yml@1.0.0
      secrets:
        NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
